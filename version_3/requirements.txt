# Core API
fastapi==0.110.0
uvicorn==0.27.1
python-multipart==0.0.9
httpx==0.27.0
pydantic==2.6.4

# STT
faster-whisper==1.0.1

# RAG
faiss-cpu==1.7.4
requests==2.31.0
numpy==1.26.4
PyPDF2==3.0.1

# LangChain (for text splitting)
langchain==0.2.16
langchain-community==0.2.16

# Audio
sounddevice==0.4.6
pyaudio==0.2.14

# TTS
# espeak-ng is a system package — install via: sudo apt install espeak-ng -y

# Utilities
tabulate==0.9.0
tqdm==4.66.2
